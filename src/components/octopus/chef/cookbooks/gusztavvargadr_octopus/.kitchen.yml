---
driver:
  name: vagrant
  vagrantfiles:
    - ../../../../../../Vagrantfile.kitchen
  synced_folders:
    - ["test", "/kitchen-test"]

provisioner:
  name: chef_solo

verifier:
  name: inspec

platforms:
  - name: w
    transport:
      name: winrm
    driver:
      box: gusztavvargadr/w16s-sql14d

suites:
  - name: default
    run_list:
      - recipe[gusztavvargadr_octopus::server]
      - recipe[gusztavvargadr_octopus::tentacle]
      - recipe[gusztavvargadr_octopus::client]
    attributes:
      gusztavvargadr_octopus:
        server:
          execute_username: vagrant
          execute_password: vagrant
          web_username: vagrant
          web_password: Vagrant42
          import:
            C:\\kitchen-test\\integration\\default\\import:
              password: Vagrant42
        tentacle:
          execute_username: vagrant
          execute_password: vagrant
          server_api_key: API-6ENORPWQM3NWT7G6P47W5USM
          environment_names:
            - environment
          role_names:
            - role
