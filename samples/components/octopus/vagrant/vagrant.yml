server:
  run_list:
    - role[server]

tentacle:
  instance_count: 1
  run_list:
    - role[tentacle]
  json:
    gusztavvargadr_octopus:
      tentacle:
        server_api_key:
        server_thumbprint:
