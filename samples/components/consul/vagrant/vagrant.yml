server:
  instance_count: 3
  consul:
    encrypt:
    acl_agent_token:

    acl_master_token:

client_ubuntu:
  instance_count: 1
  consul:
    encrypt:
    acl_agent_token:

    acl_client_token:

client_windows:
  instance_count: 1
  consul:
    encrypt:
    acl_agent_token:

    acl_client_token:
