server:
  instance_count: 3
  consul_server:
    json:
      gusztavvargadr_consul:
        server:
          config:
            options:
              encrypt: ''
              acl_agent_token: ''
              acl_master_token: ''

client_ubuntu:
  instance_count: 1
  consul_client:
    json:
      gusztavvargadr_consul:
        client:
          config:
            options:
              encrypt: ''
              acl_agent_token: ''
              acl_token: ''

client_windows:
  instance_count: 1
  consul_client:
    json:
      gusztavvargadr_consul:
        client:
          config:
            options:
              encrypt: ''
              acl_agent_token: ''
              acl_token: ''
